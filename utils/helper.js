const addDaysToCurrentDate = function (day) {
  const currentDate = new Date();
  const addingDay = day;
  const result = someDate.setDate(currentDate.getDate() + addingDay);
  return new Date(result);
};

// unused code but can be used in future
